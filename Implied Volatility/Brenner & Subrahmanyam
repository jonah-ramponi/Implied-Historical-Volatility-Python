#A very simple implementation of the method discussed by Brenner & Subramanyam, a simplistic approach to determine I.V. 

import numpy as np

p = #market price for the option
T = #number of days till expiry
S_0 = #initial price

def bren_sub(T,p,S_0):
    v = np.sqrt(2*np.pi/T)*p/S_0
    return v

v = bren_sub(T,p,S_0)
