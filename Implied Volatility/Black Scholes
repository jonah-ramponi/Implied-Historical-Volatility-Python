#A simple implementation of Black Scholes, determining the price of an option for a given volatility, v. I use this to check implementations
#of the other techniques. 

call = 1 #it is a call
put = -1 #it is a put
r = #interest rate
T = #time to maturity
v = #volatility
S_0 = #initial stock price
K = #strike price


def blackScholes(type,S_0,K,T,r,v):
    d1 = (np.log(S_0/K)+(r+0.5*v**2)*T)/(v*np.sqrt(T))
    d2 = d1 - v*np.sqrt(T)
    p = sp.norm().cdf(d1)*S_0 - sp.norm().cdf(d2)*K*np.exp(-r*T)
    return p

p = blackScholes(type,S_0,K,T,r,v)

